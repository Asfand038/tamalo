import styled from 'styled-components';

export const StyledSidebar = styled.div`
  .sidebar > div {
    position: relative;
    width: 240px;
    z-index: -1;
  }
  .sidebar-item-icon {
    min-width: 30px;
  }
`;
